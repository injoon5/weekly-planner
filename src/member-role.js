/** @deprecated Import from `./roles.js` — kept for existing imports/tests. */
export {
  normalizeMemberRole,
  isEditorRole,
  MEMBER_ROLE,
  BOARD_ROLE,
  SHARE_MODE,
  SHARE_ROLE,
  normalizeShareMode,
  normalizeShareRole,
} from './roles.js';
