---
title: Print
description: Print dialog, local print preferences, light-only print styling, and mobile image fallback.
---

# Print

## Prefs (`print-prefs.js`)

Key: `weekly-planner.print:v1` (legacy `weekly-planner.print` is still read).

- Name/time fields normalized to 40 chars
- Booleans default true unless explicitly false
- Board `from`/`to` are resolved transiently — **not** persisted

## Flow (`usePrintSetup` + `PrintDialog`)

Open dialog → edit draft → write normalized prefs → print **or** image export.

`canNativePrint()` (`print-support.js`) is false when:

- `window.print` is missing
- Installed PWA / standalone (or `minimal-ui`) display mode
- Known in-app browsers (KakaoTalk, Instagram, Facebook, Line, Naver in-app, Android `wv`)

**Native path:** `prepareHeadTracksForPrint()` → `window.print()`.

**Fallback path:** force `@media print` onto the screen (`print-media.js`), capture `[data-app-shell=planner]` via `html-to-image` (`print-image.js`), then Web Share the PNG or download it (`print-share.js`). Dialog title/CTA switch to “이미지로 저장”.

`PrintMeta` renders print-only header fields (blank underline when enabled but empty).

## Style invariants

- Print uses light palette even in dark mode
- Popovers / dialogs / drawers / toasts hidden in print CSS
- Grid print rules avoid clipped headers; CSS vars instead of pixel-locked header dims where needed
